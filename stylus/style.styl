@import nib;

global-reset();

@import colors;
@import typography;

html {
  height: 100%;
}

body {
  background-color: c_steel;
  height: 100%;
}

.main-interactive {
  max-width: 960px;
  margin: 0 auto;
  padding: 0 10px;
}

.credits {
  background-color: rgba(255,255,255,.6);
  padding: 10px 20px;
}

hr {
  width: 200px;
  margin: 0 auto;
  border: 1px solid #999;
}

@import interactive;
